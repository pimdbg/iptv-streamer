# iptv-streamer

### Prerequisites
- Node Version v22.21.0
- NPM Version v10.9.4

### Installation
- Install all packages, make sure both in the root as in the ./renderer directory are ready
- Setup the environment file by running ```cp env.example .env``` in the root directory
- Run ``` npm run dev ``` to start the development environment